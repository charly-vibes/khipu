pre-commit:
# ah:managed:start
  ah-check:
    run: ah check
# ah:managed:end

  parallel: true
  commands:
    lint:
      run: just lint
    typecheck:
      run: just typecheck

pre-push:
  commands:
    tests:
      run: uv run pytest
